
# ⭐ EasyRaceW — AI-Advanced Racing Game

**EasyRaceW** — это расширяемая гоночная игра с поддержкой **искусственного интеллекта (AI)**. Игра позволяет:

* запускать гоночный симулятор,
* обучать AI-агента управлять машиной,
* тестировать качество модели на различных трассах.

Проект содержит:

* игровой движок (`main.py`),
* скрипт для обучения AI (`train_ai.py`),
* программа для тестирования AI (`test_ai.py`),
* набор **трасс** (`tracks/`),
* набор **моделей** (`models/`),
* возможность поменять машину и фон меню **ассеты** (`assets/`).

---

## 🚀 Начало работы

### 📦 Требования

Убедись, что у тебя установлено:

* Python 3.8+
* pip

---

## 🛠 Установка зависимостей

1. Скачай zip проекта:

Скачай проект и распакуй

2. Установи зависимости:

```bash
pip install -r requirements.txt
```


## ▶️ Как играть самому

```bash
python main.py
```

Это откроет главное меню игры. Ты можешь прокатиться и поставить свои рекорды на существующих трассах, или создать свою!

---

## 🤖 Обучение AI

Чтобы обучить искусственный интеллект, используй:

```bash
python train_ai.py
```

Скрипт `train_ai.py` отвечает за:

* загрузку данных/состояний игры,
* обучение модели,
* сохранение промежуточных и итоговой модели в папку `models/`.

Ты можешь настраивать параметры обучения (скорость, количество эпизодов, архитектуру модели и пр.) в теле скрипта.

---

## 🧪 Тестирование AI

Чтобы протестировать обученную модель AI:

```bash
python test_ai.py
```

Скрипт запускает симуляцию с уже обученным агентом c его особенностями поведения.

что бы запустить конкретную модель:

```python
#75 строка
def run_ai_game(track_path="tracks/track_05.json", model_path="models/racer_model_1275000_steps.zip")
                                                              поменять номер модели  ⬆
```

---

## 📁 Структура проекта

```
EasyRaceW/
├── assets/          # Графика, звуки, ассеты для игры
├── models/          # Обученные модели AI
├── tracks/          # Файлы трасс
├── main.py          # Игровой движок
├── train_ai.py      # Обучение AI
├── test_ai.py       # Тестирование AI
├── requirements.txt # Зависимости
└── README.md        # Этот файл
```

---

## 🧠 Архитектура AI

В проекте используется подход PPO (Proximal Policy Optimization) - разновидность Обучения С Подкреплением

---

## 💡 Идеи для будущих улучшений

* Поддержка гонки ИИ с игроком
* Добавление магазина авто
* Добавление тюнинга
* Добавление ИИ для генерации трасс

---

## 📜 Лицензия

Этот проект открыт для модификаций и использования.
